<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script
      src="https://kit.fontawesome.com/5e31a87df8.js"
      defer
    ></script>
    <link rel="icon" href="/images/fileImg.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/login.css" />
    <title>Lizzy's Delivery Depot📦</title>
  </head>
  <body class="container">
    <div class="form-box">
      <h1 id="title">Login</h1>
 
<ul>
      <% if(messages.success_msg){ %>
<li><%= messages.success_msg %> </li>
     <% }  %>
    </ul>

    <ul>
      <% if(messages.error){ %>
<li><%= messages.error %> </li>
     <% }  %>
    </ul>
      <form action="/login" method="POST">
        <div class="input-group">
          <div class="input-field">
            <i class="fa-solid fa-envelope"></i>
            <input type="email" placeholder="Email ID" required name="email"/>
          </div>
          <div class="input-field">
            <i class="fa-solid fa-lock"></i>
            <input type="password" placeholder="Password" required name="password"/>
          </div>
          <p>Lost Password? <a href="/forgot-password">Click here to reset</a></p>
          <br>
          <p>Don't have an account? <a href="/register">Register here</a></p>
        </div>
        <div class="btn-field">
          <button type="submit" id="signInBtn">Sign In</button>
        </div>
      </form>
    </div>
  </body>
</html>
